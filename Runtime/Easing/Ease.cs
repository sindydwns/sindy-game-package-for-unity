namespace Sindy.Easing
{
    /// <summary>
    /// FR-EASE-02. 표준 Easing enum.
    /// 이름은 Sindy.Easing 네임스페이스 안에서 사용되므로 Ease 접두사 없이 정의된다.
    /// (예: Ease.OutCubic == 명세의 EaseOutCubic)
    /// </summary>
    public enum Ease
    {
        Linear,
        InQuad,
        OutQuad,
        InOutQuad,
        InCubic,
        OutCubic,
        InOutCubic,
    }
}
